module.exports = mongoose => {
	const Omax = mongoose.model(
		"omax",
		mongoose.Schema(
			{
				id: Number,
 				name: String,
 				operator: String,
 				currentRuntime: Number,
 				isActive: Boolean,
 				tubeRuntime: Number,
 				headRuntime: Number,
			}
		)
	);

	return Omax;
}