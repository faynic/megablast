import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MachinePartsAddComponent } from './machine-parts-add.component';

describe('MachinePartsAddComponent', () => {
  let component: MachinePartsAddComponent;
  let fixture: ComponentFixture<MachinePartsAddComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MachinePartsAddComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MachinePartsAddComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
